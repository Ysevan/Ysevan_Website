# old：早年的网站、素材和收藏

2026-09-24 至 25 整理。详细盘点（每一项的来源、原作者、能不能跑）见 Ysevan 控制室里的《old 项目清单》。

## 目录

old/ 本身就是网站，结构和原来的 Ysevan_Website（github.com/Ysevan/Ysevan_Website）一样；另加一个「网站素材」文件夹放素材。

```
old/
├── index.html              绿色「主页」：总主页。左边搜索框 + 三个入口「博客首页、游戏中心、刷刷软件」，右边只留背景插画；手机上左栏铺满整屏
├── css/home-fix.css        首页补丁：高清背景图、搜索结果样式、手机布局、播放器样式
├── js/site-search.js       首页搜索（79 条：页面、游戏、刷刷、旧站、素材），不依赖 jQuery，双击打开也能搜
├── js/bgm.js               首页背景音乐：蓝心羽《阿拉斯加海湾》，自带播放器，能调音量
├── img/p1543-4k.jpg        首页背景高清版（3840×2400，AI 放大自原图 img/p1543.jpg，原图保留）
├── img/bgm-cover.jpg       播放器封面（原来每次打开都去网易云取，现在放本地）
├── music/                  放背景音乐文件：阿拉斯加海湾.flac（最好）或 阿拉斯加海湾.mp3。会随仓库公开（有版权，屋主定的）
├── 404.html、404-game/     三体文案 + 小恐龙的 404 页
├── Blog/
│   ├── Home.html           封面：Welcome to EVa's Home + 自己写的 7 句话
│   ├── Explore.html、Blog.html、ACGN.html
│   ├── 2class/             高一（2）班回忆录
│   ├── miku1/              MIKU 的避风港
│   └── Blog2/              Blog 页左上角站名链过去的 Hipster Cards 博客页（GitHub 版原来弄丢了，已补回）
├── game/                   31 个小游戏，入口 Game.html
├── EVa/
│   ├── css/、js/           原有
│   └── 2021-EVs-world/     第一个个人站「EV's world」（2021-08-29 建，evyan.top）
├── shua/                   刷刷刷题软件 v3.12.0（从 tools/shua 导出的一份，首页「刷刷软件」指向这里）
├── 网站素材/               ← 新加的文件夹
│   ├── index.html          素材总览页
│   ├── 节日与表白/、特效/、小游戏/
│   └── 网页模板/           收集的网站模板和博客源码
├── img/touxiang-2022.png   2022 年用的旧头像（抱奶茶的小熊）
├── 打开老站.command        Mac 双击：起本地服务器，打开 Blog/Home.html
├── 打开老站.bat            Windows 双击：同上（配套 open-old-site.ps1）
├── 说明.md                 本文件
└── 其余                    原有：css/ js/ sass/ fonts/ img/ background/ static/ tools/ AGENTS.md 等
```

**怎么看**：推荐双击 `打开老站.command`（Mac）或 `打开老站.bat`（Windows）。它会在本机起一个只读的本地服务器，然后打开 Blog 首页，关掉那个窗口就停了。直接双击网页也能看大部分内容，但打飞机、立方体跳跃这两个游戏双击打开时，浏览器会以跨域为由拦下图片，起不来。

**导航**：Blog 下四个页面的导航栏是 `Home Explore Blog ACGN ｜ 网站素材 Game [主页]`。

改名脉络：EV / EVa（2021）→ 书生子白（2022）→ Ysevan（2023 起）。

old/ 同时是那个公开仓库的 git 克隆（隐藏的 `.git/`）。整理后的内容已于 2026-09-25 全部提交并推送到 GitHub，本地没有只留在本机的内容了，换设备 clone 下来就是完整的一份。

### 公开仓库里有什么

全部内容都在公开仓库里，包括别人的网页模板（`网站素材/网页模板/`，293 MB，屋主定的）和 `music/`。私人合照、表白相册、EV 初稿、Lover 都已删除。`Blog/2class/` 里的班级照片和花名册在公开仓库里，这是屋主定的。

注意：`music/` 现在是空的。以后把《阿拉斯加海湾》放进去再推送，这首歌就会公开在 GitHub 上（有版权，可能收到下架通知）。

### 相对 GitHub 版改了什么

- `Blog/` 四个页面的导航栏（每页 4 行）：
  - 右边加「网站素材」
  - 左右两栏宽度改成左 5/12、右 4/12，「网站素材、Game、主页」在 800–1700 宽都排在同一行（原来 1200 以上和 800 左右「主页」会掉到第二行）
  - 左上角站名：Home、Explore、ACGN 三页链到 Home 页，Blog 页链到 Blog2（原来都是空链接）
- `index.html`（首页）：第三个入口「哔哩哔哩」换成「刷刷软件」（指向 `shua/index.html`）；右边的「小游戏」列表整块删掉，只留背景插画（游戏从左边「游戏中心」进）
- `favicon.ico`：原来是一张 750×750、263K 的 JPEG 改了后缀，换成真正的图标文件（同一张头像，16–64 像素四档，17K）
- 其余全是新增：`网站素材/`、`Blog/Blog2/`、`EVa/2021-EVs-world/`、`img/touxiang-2022.png`、`img/p1543-4k.jpg`、`img/bgm-cover.jpg`、`css/home-fix.css`、`js/site-search.js`、`js/bgm.js`、`shua/`、双击启动器、本说明。

## 网站素材（`网站素材/`）

当年给网站攒的素材。入口是 Blog 四个页面导航栏里的「网站素材」，也可以直接双击 `网站素材/index.html`。总览页按类别列出全部 39 项，写了效果、原作者、用在了哪个站，点卡片在新标签页打开。总览页的排版照着「Ysevan的小屋」做：暖纸底、橄榄墨、行楷标题、宋体卡名，每个大类一块灰毯，夜里跟着系统换成小屋的夜色。

真正用进站里的只有 4 样：心跳6（EV's world 的 blog）、绘制银河（EV's world 的 miku1）、跨年烟花和图片立方体轮播（书生子白的 miku1）。其余都是攒下来还没用上的。

### 节日与表白

| 文件夹 | 是什么 |
|---|---|
| `国庆快乐/` | 2019 国庆：CSS 国旗 + 烟花 + 音乐，按钮文案和音乐是自己加的 |
| `圣诞雪花/` | 纯 CSS 3D 圣诞树，加了圣诞歌 |
| `圣诞树/` | GSAP 圣诞树，2024-12 改了祝福语送人 |
| `生日祝福/` | 开源生日祝福页（AJLoveChina/birthday）：点按钮开灯、挂横幅、放气球、端蛋糕、点蜡烛 |
| `跨年烟花/` | Happy 2020 烟花 |
| `过年烟花/` | 烟花拼字。拼的字写死成「17sucai」水印，改成祝福语就能用 |
| `心跳合集/` | 6 个爱心特效 |
| `表白小程序.exe` | 网上流传的 VB6 表白程序 |

### 特效

- **星空宇宙/**：璀璨群星（WebGL 星空）、超光速粒子（按住鼠标跃迁）、银河系星云（three.js）、绘制银河（鼠标撒星星）、能量射线、传送门（p5.js）、时空隧道（three.js 粒子隧道）、时间隧道（彩虹线隧道）
- **粒子/**：粒子旋涡、粒子爆炸（three.js GPU 百万粒子）、离子清理工（吸引/排斥连线粒子）、花里胡哨的粒子（点击变形）、喷泉（百万粒子）、粒子烟火（Babylon.js）
- **其他/**：线条花（万花尺）、喂鱼啦、闪电风暴雨、图片爆炸轮播、图片立方体轮播
- **费米悖论-64k-demo/**：Mercury 小组 2016 年的 64KB Windows demo，不是网页

注意：银河系星云、粒子爆炸、喷泉很吃显卡，没独显的机器会卡。

### 小游戏

打飞机、立方体跳跃（Construct 2 商业模板，立方体跳跃带 .capx 工程源文件）、箭头防御、箭头雨、解压小游戏、金山打字小游戏（缺三张背景图，能玩）。

### 网页模板（`网站素材/网页模板/`）

| 文件夹 | 是什么 |
|---|---|
| `Hipster-Cards个人主页/` | Creative Tim 的卡片风个人主页模板，Blog2 用的就是它 |
| `海拥的博客-wanghao221.github.io-master.zip` | CSDN 博主海拥的 Hexo 博客全站，书生子白站里的小游戏、表白合集、404 恐龙都出自这里 |
| `godbasin的博客/` | 前端博主 godbasin 的博客，首页路径已改成相对路径，能直接双击打开 |
| `Hexo主题-quark-main.zip` | Hexo 博客主题 quark（Pcrab） |
| `编程导航-code-nav-master.zip` | 鱼皮（liyupi）的「编程导航」完整项目源码 |
| `手机网页48套/`、`中文网页16套/` | 2016 年的国内网页模板合集 |

## 整理记录

**2026-09-25 深夜：删掉私人合照和表白合集**
- 屋主看过 12 张私人合照后说删掉，用到它们的网页也删：`game/love/`、`game/lover/` 两份完全相同的「表白合集」（合集首页 + 11 个表白页，其中「表白相册」放的就是这 12 张照片）整个移到废纸篓 `old-整理删除-2026-09-25/深夜-私人合照与表白合集/`，清空前都能拖回来
- 公开仓库里这两个文件夹一起删掉（照片本来就没进过仓库，历史里的也在 09-24 清掉了）；首页搜索去掉「表白合集」一条（80 → 79）；本地忽略清单对应两行删掉
- 聊天里截过的图和缩略图、草稿区里清理前的仓库备份包与仓库副本、测试浏览器缓存一并删除

**2026-09-25 深夜：删掉表白相册和 EV 初稿**
- 屋主看过内容后说没用了：`网站素材/节日与表白/表白相册/`（相册页 + 7 张真人照片）和 `EVa/2021-EVs-world-初稿/`（2021 年模板改的第一版主页，标题和自我介绍里有实名）移到废纸篓 `old-整理删除-2026-09-25/深夜-表白相册与EV初稿/`，清空前都能拖回来。两者都从没进过公开仓库
- 网站素材总览页去掉「表白相册」那张卡（40 → 39 项）；本地忽略清单里对应的两行也删了

**2026-09-25 深夜：首页背景变高清、打开变快**
- 背景图：原图 `img/p1543.jpg` 只有 1920×1200，屋主台式机（2040 宽 @1.25 倍）要 2550 像素、笔记本要 3830 像素，所以糊。整个 old/ 里没有更大的原图，用 Real-ESRGAN（x4plus-anime 动漫模型）放大成 `img/p1543-4k.jpg`（3840×2400，1.5 MB）；和普通放大、另外两个模型并排比过，这个线条和铁丝网最清楚；颜色和原图逐像素平均差 2（满分 255），没有偏色。只在 `css/home-fix.css` 里换了首页的背景地址，主题 `style.css` 没动，原图也留着。放大工具用完已删
- 打开慢的原因和处理（2040 宽实测，页面加载完成从 3.5 秒降到 0.24 秒，控制台零报错，打开时零外部请求）：
  - busuanzi 访客计数脚本（`dn-lbstatics.qbox.me`）：服务早已失效，每次卡 1.5 秒还被浏览器拦下，数字从来没显示过 → 脚本和页脚那块计数一起删掉
  - 网易云外链播放器：会拖进来一整套网易云的 CSS/JS，页面要等它才算加载完 → 改成访客第一次点页面/按键时才插进去（浏览器本来也要等这一下才允许出声），并加了 `allow="autoplay"`，点完就能自己放
  - 网易云「外链音频地址」这首歌返回 404 → 从音源里去掉
  - 播放器封面原来每次去网易云取 → 存成本地 `img/bgm-cover.jpg`
  - `js/main.js`（要 RequireJS，页面没加载，一直报 `require is not defined`）、`fancybox` 样式（配套脚本从没加载）、四段 jQuery 脚本（随机背景、图标提示气泡、TAG 开关、评论箭头；页面没加载 jQuery，从来没跑过）→ 删掉；右侧已隐藏的「TAG 开关」和上下箭头元素也一起删了
  - `favicon.ico` 原来是 263K 的 JPEG → 换成 17K 的真图标
- 手机（417 宽 @3.5 倍）也用高清背景，加载 0.24 秒、零报错

**2026-09-25 晚：背景音乐换成《阿拉斯加海湾》，能调音量**
- 左下角换成自己的播放器（`js/bgm.js`）：封面、歌名、播放/暂停、进度条、音量条；默认音量 30%，调过的音量会记住
- 打开页面就试着播放；浏览器不允许没点过页面就出声，被拦的话，访客在页面任意位置点一下就开始放；自己按了暂停，点别处不会又自己放
- 音源顺序：`music/阿拉斯加海湾.flac` → `music/阿拉斯加海湾.mp3` → 都没有就退回网易云外链播放器（「外链音频地址」一档后来发现返回 404，已去掉；网易云播放器后来改成访客第一次点页面时才插入，见上一条）（屋主给的外链代码，宽 330，歌曲 id 1500569811）。退回那一档能放，但音质由网易云定、也调不了音量
- **要最好的音质和能调音量，就把歌放进 `music/`**：FLAC 最好，其次 320k MP3。网易云客户端下载的 `.ncm` 文件是加密的，浏览器放不了
- `music/` 已写进 `.git/info/exclude`，不会推到公开仓库（后来屋主定了要推，已从忽略清单去掉）
- 实测：没有文件时退回网易云播放器（现在是点一下页面后才出现）；有文件时，浏览器拦截自动播放→点页面就放、允许自动播放→打开就放；音量条、记住音量、暂停后不自己恢复都验证过；手机上播放器在底部正中，不压页脚

**2026-09-25 晚：首页搜索、手机端、音乐、页脚**
- 页脚原来写的是实名拼音 →「by Ysevan」
- 搜索重写成 `js/site-search.js`：原来的搜索靠 jQuery（页面从没加载）去读 `/search.json`（空的，而且是绝对路径，本地打开找不到），所以一直是坏的；底部另一段 Swiftype 搜索找的 `#search` 元素页面上没有，还带着第三方 key，一并删掉。新搜索数据写在 js 里，80 条，从 `game/Game.html` 和 `网站素材/index.html` 提取（私人的表白相册不收）；以后加了游戏或素材要重新生成这份数据
- 手机端：原来缺 `<meta viewport>`，而且主题在窄屏上把左栏藏掉、指望一个要 jQuery 的手机导航顶上，于是手机上什么都没有。加了 viewport 和 `css/home-fix.css`：左栏铺满整屏竖排、音乐在底部正中、页脚贴底。390 和 417 两个宽度实测无横向溢出
- 右侧的「TAG 开关」和上/下箭头隐藏了：它们原本是给删掉的小游戏列表用的（后来连元素一起删了）
- 音乐：网易云外链播放器换成小咪《我走后》（歌曲 id 1370047789），链接从 http 改成 https（http 放在 https 站点上会被浏览器拦）。**这首在网易云是 VIP 歌曲**（fee=1），外链播放器能显示歌名，但未登录 VIP 的访客多半放不出声音；这台机器上连原来那首也测不出声音，所以能不能放要在自己电脑上点一下确认
- 页面里还剩几段依赖 jQuery 的旧脚本（随机背景、图标提示气泡），控制台会报 `$ is not defined`，这是改之前就有的，不影响上面这些功能（后来已全部删掉，首页控制台现在零报错）

**2026-09-25 晚：首页入口换成刷刷、去掉小游戏列表**
- `shua/`（原名 `刷刷/`，2026-09-25 改成英文，网址好记好打）：用 `git archive` 从 `tools/shua` 导出当时已提交的 v3.12.0（55 个文件，5.2 MB，不带 git 历史）。这是一份快照，`tools/shua` 以后更新不会自动同步过来，要更新就再导一次。屋主说刷刷可以公开，所以没放进忽略清单
- 首页第三个入口「哔哩哔哩」→「刷刷软件」；右边的小游戏列表删掉（87 行），只留背景插画
- 验证：刷刷双击打开和经启动器打开都正常；首页三个入口都有效
- 已知老问题（不是这次改的）：首页在手机上本来就是坏的——主题的手机布局要靠 jQuery，而页面从来没加载 jQuery（控制台报 `$ is not defined`）（后来已修：手机布局改用 `css/home-fix.css`，jQuery 脚本也删了）

**2026-09-25 晚：生日祝福解压成网页**
- `生日祝福-birthday-master.zip` 解压成 `节日与表白/生日祝福/`，zip 移到废纸篓 `old-整理删除-2026-09-25`
- 页面原来用 less.js 现场读 `cake.less`，双击打开时会被浏览器拦下、蛋糕出不来；改成直接引用同目录里编译好的 `cake.css`（同一份样式），双击就能完整跑完

**2026-09-25 晚：去掉送人作品里的名字、原作者补署名**
- 圣诞树：文件夹名里原来带对方的名字，改名为 `圣诞树`，第一句祝福里对方的名字改成「亲爱的你」
- 表白相册：6 张照片原来用对方的真名命名，改成 `照片1–6.jpg`，样式表里的引用一起改了
- 素材总览页：原作者不知道的 28 项（原来写「不详」、只写了「CodePen / GitHub开源」、模糊来源，以及自己拼装的国庆快乐）统一写 Ysevan；有名有姓的和标了「推测」的不变

**2026-09-25 晚：删掉 Lover**
- 屋主说没用了，`Lover/` 移到废纸篓 `old-整理删除-2026-09-25`；没有任何页面链到它

**2026-09-25 晚：old/ 本身改成网站结构**
- 把项目从 `个人网站/Ysevan_Website-master/` 整体上移到 old/ 根目录，外面两层空文件夹删掉；「网页模板」收进「网站素材」
- 验证：改过和新加的页面 199 个本地链接全部有效；启动器从新位置实测能打开首页、Blog2、网站素材、游戏；Blog 四页导航 6 个宽度全部同一行

**2026-09-25：整合进 Ysevan_Website-master 的结构**
- 以 GitHub 上的 Ysevan_Website 为底（克隆，原有内容不动），把网站素材、Lover、EV's world（含初稿）、Blog2、旧头像、双击启动器放进它的结构里；12 张私人合照放回 `game/love`、`game/lover` 原位
- `2022-书生子白` 这份 2022 年原样快照移到了废纸篓 `old-整理删除-2026-09-25`：它独有的屋主内容（Blog2、私人合照、旧头像）都已整合；其余是博主海拥的 Hexo 博客文章（GitHub 版 2026-05 整理时特意删掉的，原件在「网页模板」的海拥博客压缩包里），以及被 GitHub 版改过的旧版文件
- Blog2 修了两处：两个指向不存在的 `Blog/game/` 的游戏链接改成 `../../game/`；缺的 3 张模板示例图从「网页模板/Hipster-Cards个人主页」复制补齐
- 验证：Blog 四页导航在 6 个宽度 × 4 页全部同一行；改过和新加的页面 181 个本地链接全部有效；启动器实测能打开首页、Blog2、网站素材、游戏、Lover、EV's world

**2026-09-24：按内容分类、去重**
- 按内容重新分类；节日与表白、特效、小游戏后来收进了网站素材
- 理顺了几个坏页面：时空隧道、时间隧道的脚本放回各自的文件夹；闪电特效、箭头雨、国庆快乐、线条花的入口页从 .txt 改成 .html；打字游戏的 `demo.txt` 改成 `index.html`；喂鱼啦、火焰传送门、粒子烟火、时间隧道从 GBK 转成 UTF-8
- 移到废纸篓的（`old-重复副本-2026-09-24`、`old-整理删除-2026-09-24`，清空前都能拖回来）：
  - 重复的：16 个和解压版完全一样的 zip/rar；游戏源码里的能量射线、星空、箭头射击；首页模板（和海拥博客的 zip 一样）；godbasin 的 zip；`代码/` 里和别处重复的片段
  - 打不开也修不回来的：`轮播相册/` 里的两个 Codrops 演示页；3D 旋转木马；粒子爆炸旧版片段
  - 空的：`网站/刷刷/.tmp/…`
  - 系统缓存：43 个 `.DS_Store` 和 `Thumbs.db`
  - 按屋主要求删的：`学习资料/`（袁老师 HTML+CSS 课程、2022 网页笔记）、`_备份/`（仓库清理前的完整历史包）
- GitHub 公开仓库 Ysevan/Ysevan_Website 清理：从全部历史里删掉了 Lover 页、24 张私人合照、带 cookie 的 `js/back.js`，以及班级页里的 QQ 群号；班级照片和花名册按要求保留
- 考虑过压缩：网页模板 86% 本来就是 zip/rar，个人网站主要是照片和音乐，实测压缩只省 5–15%，还没法双击看页面，所以不压
